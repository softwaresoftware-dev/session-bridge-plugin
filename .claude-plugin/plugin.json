{
  "name": "session-bridge",
  "description": "Channel MCP for session-bridge mesh \u2014 enables bidirectional communication between Claude sessions. Bundles the FastAPI bridge daemon; installs it via daemon-manager on first setup.",
  "version": "0.2.0",
  "author": {
    "name": "NOV LLC",
    "url": "https://github.com/softwaresoftware-dev"
  },
  "repository": "https://github.com/softwaresoftware-dev/session-bridge-plugin",
  "keywords": [
    "session",
    "channel",
    "mesh",
    "communication"
  ],
  "mcpServers": {
    "session-bridge": {
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/channel.mjs"
      ],
      "cwd": "${CLAUDE_PLUGIN_ROOT}"
    }
  }
}
